// 组件 —— 本章各站共用的小件。眼下只有摆放引擎的注入配；站自己的零件放自己的模块，
// 第二个站也要用了再挪进来（共享件的家规，方向是「用了两次才上桌」）。

import { 台词, 模板 } from './台词表.js';

/**
 * 本章递给 /shared/js/摆放.js 的注入配（引擎签名见其头注：玩一轮({挂点, 题, 配, 走查})）。
 * 04 是汽笛🔔小火车，我们是营地开饭铃🛎——图标就是孩子的说明书，标签只给读屏。
 * 话术全是函数：说话那一刻才取词，语言活切换（台词表 合() 视图的本意）。
 */
export const 摆放配 = {
  提交钮: { 图标: '🛎', 标签: { cn: '摇开饭铃', en: 'Ring the dinner bell' } },
  话术: {
    对: () => 台词.摆放.对,
    错1: () => 台词.摆放.错1,
    提示头: () => 台词.摆放.提示头,
    演示头: () => 台词.摆放.演示头,
    教数: (n) => 模板.摆放.教数(n),
  },
  上限: 18, // 一个容器最多摆几个 —— 松果虫虫站的顶（6 只松鼠 × 每只 3 颗）
};
