# 2026-08-01 · 防呆评审：把「世界乱来」的口子挨个焊上

10 个提交。起因是一位资深前端看过线上站后的一句话：「整体很不错，防呆设计有些还不完善」。
于是按 poka-yoke 维度全站过了一遍代码，开出总览 + 8 张票（`975e28a`），当天修完 7 张
（08 主动不修）。

评审结论一句话：这个站**防「孩子乱来」防得相当好**（连点、误触、乱切基本都有真机制挡着），
**防「世界乱来」还有缺口**——外部依赖挂起而非报错、设备中途消失、系统级对话框。共同根因：
**降级只在 catch 里触发，永远等不到「不返回」这种失败形状。**

修复清单：

- 备选钮补在途锁，降级台词不再骗孩子有按钮，听一句失败也出声（`7f8e8e8`）
- 第2讲纸样打印改两步确认；摄像头开不了出声、授权迟到不再盖屏（`9d0efb3`）
- 录音看门狗：墙钟 + track.onended 两条兜底腿，设备掉线不再永久「录音中」（`0d46709`）
- 第3讲进关双重创建与重来确认悬留两个竞态窗口关死（`a88041f`）
- DashScope 四类调用显式超时：卡死最多废一次请求、归 502、合成锁不再连坐（`7a2bc7c`）
- 音效发声前自愈：非 running 趁当次手势 resume，bfcache 恢复不再永久静默（`538f21f`）
- 全站前端请求超时安全网：卡住也落进既有降级路，兼守 speechSynthesis 缺失末端（`aea8a14`）

另落两份文档：性能优化方案与实测效果（`9550de7`）、CLAUDE.md/AGENTS.md 补记缓存分流
与预热（`420d3ba`）。
