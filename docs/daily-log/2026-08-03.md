# 2026-08-03 · 账号体系：从 grilling 到公网换门，一天走完

2 个提交（`03c0d34` 开票、`8c861ba` 落地），加上未随码提交的上线操作。这是又一次
「spec 当天定、票当天清」，也是并行 subagent 工作流最顺的一次。

**上午：设计。** grilling 九项决策逐条拍板——邀请码准入（预生成 10 个）、纯短信无密码
（整块密码代码不存在）、180 天滑动会话、彻底 1:1 不建孩子表、进度不透明 jsonb 整包
（护住「加一讲零后端改动」）、每讲导出合并函数 + 重置戳（星星只增不减、重来不诈尸）、
`AUTH_MODE` 开关（缺省 off 本地零依赖）、验证态入 Postgres 两张小表（不上 Redis）、
阿里云资源全套复用。spec + 8 张票落 `.scratch/accounts/`。

**下午：并行实现。** 分波推进：01 骨架由主线自做、垄断全部共享文件改动 → 02（认证 API）
∥ 04（进度 API）→ 05（云同步引擎）→ 03（登录页）∥ 06（第2讲接入）∥ 07（第3讲接入）→
08（邀请码脚本 + 文档）。每张后端票 curl 对真库真验，前端合并函数 48 个新 node 测试，
全套 657 绿。两轴 code-review（家规轴 + 规格轴）揪出 4 处修掉——最值的一条：sendBeacon
只会发 POST 而端点只挂了 PUT，pagehide 兜底生而即死。

**晚上：上线换门。** ECS 开 `AUTH_MODE=on`、套着 Basic 双门自测、真手机邀请码注册登录、
摘 nginx `auth_basic`——公网守门从「一个口令」换成「手机号 + 短信 + 邀请码准入」，
孩子的星星从此跟着账号跨设备走。

**当天的坑**：发版后首开「进不去第 3 讲」——SWR 逐资源 revalidate 无模块图原子性，
新主.js 混跑旧进度.js，import 失败整讲哑死，强刷自愈。已开票
`.scratch/speedup/issues/07`（on-hold）。

**尾声：路由去中文。** `/指南`→`/guide`、`/登录`→`/login`（地址栏和日志里的
`%E7%99%BB%E5%BD%95` 转义确实难看），旧 URL 挂 301 兜底。URL 一律 ASCII 的界线从此
明确：中文只留给文件名和标识符（家规），不上 URL。开发日志 `docs/daily-log/` 也是
今天建的——一天一篇，从提交史整理。

| 提交 | 内容 |
| --- | --- |
| 03c0d34 | 账号体系开票：grilling 定稿 spec + 8 张票 |
| 8c861ba | 账号体系落地：邀请码+纯短信登录、云端进度同步、AUTH_MODE 整站门 |
