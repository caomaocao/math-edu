# 2026-07-29 · 实体图工程：85 张贴纸图全站铺完，emoji 时代结束

34 个提交。整天一件事：教学实体从 emoji 全部换成预生成的贴纸风格图。多 agent worktree
并行铺素材，也踩了合并的坑（`45878ca` 修合并弄坏的样式表——顺手立了「样式表完好」红线）。

**地基（票 02）：** 素材名与规范名分家（判对的键永远中文规范名、素材名是 WebP 文件名、
归一表多对一）、渲染收进共享层单闸 `画实体()`、覆盖测试收成单接缝 实体们 ⊆ 注册表 ⊆ 磁盘
（`afc5756`）。注册表按类留空位让并行票各插各的 hunk（`3e494fc`）——这是给 git 自动合并
让路的小设计。

**铺素材（票 03–10、批量）：** 22 只动物一次集中入库（`ab29777`）、冰箱八样（`98e2fa2`）、
衣柜九件（`aca97e7`）、学校地图十一张地标（`ea7184e`）、宝藏整关——脚印刻意改 CSS 画
（`8ecfd48`）、布景道具十二张（`5d6aced`）、第2讲水果 + 正方体不等图先有颜色（`fd82920`）。
返工也在当天：医院线稿补色、沙坑重出（`1943ad2`）、牛奶盒身去 "MILK"（`fd53294`）、
四处显小的贴纸放大（`568f29b`）。

**上锁（票 11–12）：** 关卡地图换贴纸 + 修缓存（`abccd52`）、红线落地——实体 emoji 不许
直接进 DOM，白名单每字符要写理由（`c9ff2ef`）；贴水果关自带的一套贴图退休，素材加载
全讲只剩一处（`6f89dd3`）。

**顺手的独立改进：** 拔掉拿 emoji 当键的三处（`7a10707`）、后退手势接进层级导航——
关里退地图、地图退首页，两讲共用（`9faf9de`）、首页家长角标放大成正经按钮（`cb5afdd`）、
文档对账（`b906f23`）。

（34 条明细：`git log --oneline --since=2026-07-29 --until=2026-07-30`。）
