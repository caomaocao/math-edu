# 大班数学配套网站

给一个 5 岁孩子做的语音优先数学课网站。本表只收本项目特有的领域词汇，不写实现。

## Language

### 实体与素材

**实体**：
课程里被画出来给孩子看的一个具体事物（一种动物、一处地标、一样食物、一件衣物）。它在屏幕上的样子（emoji 或实体图）与它是什么，是两回事。

**规范名**：
一个实体唯一、恒定的中文名（如「苹果」「火车站」），即判对词表的规范键。英文与别名都只是它的附属说法，不作主键。
_Avoid_: 用 emoji 字符或英文名当实体的 ID。

**教学实体**：
孩子要看着它开口说出其规范名（或别名）的实体——它本身就是这一课的词汇项。

**提示实体**：
课程语音会念出名字、但孩子回答的是别的东西（方位、行列）的实体。孩子只需认得它，不必说出它。

**素材名**：
一个*视觉*实体的唯一中文名，即实体图素材的文件名与注册表的键。**规范名 → 素材名是多对一**：同一只狐狸在几个环节里分别叫「狐狸」和「小狐狸」，两个规范名共用一个素材名、共用一张图。判对认规范名，素材认素材名，两层各管各的。
_Avoid_: 拿它当判对的键；把看着像的两样东西（小鸡与大公鸡）并成一个素材名。

**实体图**：
为一个实体预先生成、全站统一美术风格的图片素材，以素材名作键。缺图时显示回落到该实体的 emoji，但缺图本身按缺陷对待（与缺英文台词同例）。
_Avoid_: 图标（那是 UI 按钮的事）、贴图（那是渲染层的事）。

### 界面（既有约定，此处仅收词）

**UI 图形**：
不代表课程实体、只承担操作约定的图形（话筒、喇叭、开始键、国旗、彩带、奖杯）。不在实体图体系内，保留 emoji。

**母题调色板**：
一章自己的视觉世界所用的一套成体系颜色（第2讲冷灰蓝配水果色，第3讲天蓝草绿户外色）。一章一块、章章不同——全站统一的是贴纸实体图与 UI 图形约定，不是颜色。
_Avoid_: 追求全站统一主题色；把颜色写散在各处而不成一块板子。

**基准舞台**：
讲内画面的恒定逻辑坐标系：所有环节的几何都画在这一个固定尺寸的舞台上，整体等比缩放到实际屏幕。几何只定义一次，换屏幕只换缩放系数。
_Avoid_: 按屏幕尺寸逐环节重排布局；与第2讲装三维场景的那个「舞台」混为一谈（那是一个盒子，这是一套坐标系）。

**舞台坐标 / 视觉坐标**：
同一个点的两种说法。舞台坐标是元素 `left/top` 写的那套（缩放前）；视觉坐标是指针事件与元素外框报的那套（缩放后）。两者差一个缩放系数与一段补边。
_Avoid_: 把一套量到的数直接写进另一套 —— 症状不是报错，是「孩子点不中」。

**触靶**：
孩子要点、要拖的交互目标，受成文的最小尺寸约束。大人专用的钮（重来、家长入口）不是触靶——它们故意做小防孩子误点。
_Avoid_: 把「大人钮太小」当缺陷修掉。

**转屏拦罩**：
竖屏时盖在讲内的全屏图形提示，拦到手机转成横屏为止。只拦孩子界面；大人页（家长、指南、首页）竖横都可读，不拦。

### 交互输入形态

一道题只属于一种作答形态；一站的玩法用这几个词描述，不另造。

**摆放**：
数量题的动手作答：点一下往容器里加一个实体、再点已放的拿走，摆好按提交仪式交卷。答案是「摆成了什么样」，判定不听孩子说话。
_Avoid_: 拖拽（点，不是拖）。

**报数**：
孩子开口说数字作答；同一个数的各种说法（六/6/six）都算数，点击大数字瓦片是永远在场的兜底。

**配对**：
一一对应题的作答：先点一个实体、再点它的归宿，一对一对即时判。答案是「谁跟谁一对」，没有交卷动作。
_Avoid_: 仿纸面画连线。

**发饭**：
平均分题的作答：把一锅东西按每人固定份数发下去，发到锅空，再报「够分给几个人」。是摆放的变体，不是第五种形态。
